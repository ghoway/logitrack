@AGENTS.md Logic aplikasinya kurang lebih gini:
User 1. Auth (Login/Register) 2. Request Shipment 3. System Generate Tracking id & Fee (but status is pending) 4. User upload payment proof (manual, not automatic using payment gateway) Admin 1. Check if payment valid 2. If valid, admin assign the courier & update the status to in transit Courier: 1. Check Any Shipment that assign to them 2. Update shipment status (pickup, in transit, delivered) 3. Upload shipment proof (Delivered).
Update seluruh /docs/* buatkan laporan yang berisi laporan makalah seperti pendahuluan isi dan penutup. laporan tersebut bab 1 analisi sdlc, bab 2 analisis kebutuhan sistem. selanjutnya ada UML yang berisi use case diagram secara komperhensif dan text naratif use case untuk seluruh aktor pengguna. output yang di harapkan adalah narasi use case lengkap. Selanjutnya terdapat class diagram yang mencakup tipe data visibilitas atribut serta membuat squence diagram untuk logika proses pada projek ini. Output yang di harapkan adalah class dan diagram squence projek ini. bab selanjutnya yaitu implementasi back-end yang menerjemahkan diagram pada bab sebelumnya menjadi sebuah code. lalu point berikutnya adalah implementasi halaman depan atau landing page untuk menampilkan halaman utama aplikasi. karena projek ini menggunakan filament maka sesuaikan penulisan implementasi back-end dengan logic aplikasi projek ini. output file yang saya harapkan adalah makalah.MD.


@AGENTS.md Selanjutnya untuk file docs/README.MD di ubah menjadi cara penggunaan dan instalasi aplikasi di harapkan juga terdapat alur aplikasi sederhana pada file ini.